---
const { title = "Shara Mae – Engineering Portfolio" } = Astro.props;
---

<html lang="en">
  <head>
    
    <meta charset="utf-8" />
    <title>{title}</title>
    <meta
      name="description"
      content="AI, backend, Microsoft 365, and full-stack engineering portfolio of Shara Mae."
    />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <script
        src="https://code.iconify.design/iconify-icon/1.0.7/iconify-icon.min.js"
        defer
    ></script>
  </head>
  <body class="page">
    <slot />
  </body>
</html>

<style>
  /* 🌐 Global design tokens */
  :global(:root) {
    --bg: #050617;
    --card-bg: #101325;
    --accent: #ec5fa9;        /* main accent – rose */
    --accent-soft: #f9a8d4;   /* lighter rose */
    --text-main: #f9fafb;
    --text-muted: #cbd5f5;
    --border-subtle: #262b3f;
    }

  /* 🌐 Global body styling */
  :global(body.page) {
    margin: 0;
    font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
        sans-serif;
    background:
    radial-gradient(circle at 15% 0%, rgba(236, 95, 169, 0.16), transparent 45%), radial-gradient(circle at 85% 20%, rgba(129, 140, 248, 0.18), transparent 50%), linear-gradient(180deg, #8a5d81 0%, #000000c4 100%);
  color: var(--text-main);
    }

  /* 🌐 Global link styling, including visited */
  :global(a),
  :global(a:visited) {
    color: var(--accent);
    text-decoration: none;
  }

  :global(a:hover) {
    color: var(--accent-soft);
    text-decoration: underline;
  }

  /* 🌐 Shared container used by header + pages */
  :global(.shell) {
    max-width: 1120px;
    margin: 0 auto;
    padding: 1.75rem 1.5rem 4rem;
    }  

    :global(.panel) {
        max-width: 780px;
        width: 100%;
        background: radial-gradient(circle at top, #151833 0%, #050617 80%);
        border-radius: 1.1rem;
        padding: 2rem 2.25rem;
        text-align: center;
        border: 1px solid rgba(236, 95, 169, 0.5);
        box-shadow:
            0 0 0 1px rgba(236, 95, 169, 0.15),
            0 22px 55px rgba(5, 6, 23, 0.95);
        }

        /* Wrapper that centers panels on the page */
        :global(.panel-wrapper) {
        display: flex;
        justify-content: center;
        margin: 3rem 0 3.5rem;
        }

        /* 🔁 Shared card style (like your AI/Backend cards) */
        :global(.card-panel) {
        background: linear-gradient(
            180deg,
            rgba(16, 19, 37, 0.98),
            rgba(8, 10, 26, 0.95)
        );
        border-radius: 1rem;
        padding: 1.7rem;
        border: 1px solid rgba(148, 163, 184, 0.28);
        transition: border-color 0.25s ease, transform 0.25s ease,
            box-shadow 0.25s ease;
        }

        :global(.card-panel:hover) {
        border-color: rgba(236, 95, 169, 0.55);
        box-shadow: 0 14px 40px rgba(5, 6, 23, 0.9);
        transform: translateY(-2px);
        }

        /* 🔁 Shared pill/tag style */
        :global(.tag-pill) {
        padding: 0.4rem 0.95rem;
        border-radius: 999px;
        font-size: 0.76rem;
        border: 1px solid rgba(236, 95, 169, 0.45);
        background: rgba(15, 23, 42, 0.9);
        color: #f9fafb;
        letter-spacing: 0.03em;
        }

</style>
